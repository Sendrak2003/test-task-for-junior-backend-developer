# HTTP server
HTTP_ADDR=:8080
HTTP_PORT=8080

# PostgreSQL
POSTGRES_DB=taskservice
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# App database connection (for local development without Docker)
DATABASE_DSN=postgres://postgres:postgres@localhost:5432/taskservice?sslmode=disable
